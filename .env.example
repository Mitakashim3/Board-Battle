# Supabase
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ===========================================
# OAuth Configuration (Optional but Recommended)
# ===========================================
# OAuth providers are configured in Supabase Dashboard, not here.
# 
# To enable Google OAuth:
# 1. Go to Google Cloud Console → APIs & Services → Credentials
# 2. Create OAuth 2.0 Client ID (Web Application)
# 3. Add authorized redirect URI: https://YOUR_PROJECT_REF.supabase.co/auth/v1/callback
# 4. Copy Client ID and Client Secret to Supabase Dashboard
#    → Authentication → Providers → Google
#
# To enable GitHub OAuth:
# 1. Go to GitHub → Settings → Developer Settings → OAuth Apps → New OAuth App
# 2. Set Authorization callback URL: https://YOUR_PROJECT_REF.supabase.co/auth/v1/callback
# 3. Copy Client ID and Client Secret to Supabase Dashboard
#    → Authentication → Providers → GitHub
#
# SECURITY: OAuth secrets are stored in Supabase, NOT in your codebase
# ===========================================
